return {
    checkInterval = 30, -- Interval in minutes to check for new rankings
    rank = "Champion", -- The rank to give the top player
    serverNames = {
        "dev",
        "survival"
    } -- The server names to give the rank on via LP Contexts
}